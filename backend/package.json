{
  "name": "recipe-app-backend",
  "version": "1.0.0",
  "description": "MERN Recipe App Backend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ext .js --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --ext .js --fix",
    "validate": "node --check server.js && node --check models/Recipe.js && node --check routes/recipes.js",
    "test": "echo '\u26a0\ufe0f  No tests yet \u2014 skipping' && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^9.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
